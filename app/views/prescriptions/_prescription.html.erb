<%# same as the last one %>
<div id="<%= dom_id prescription %>">
  <p>
    <strong>User:</strong>
    <%= prescription.user_id %>
  </p>

  <p>
    <strong>Medication:</strong>
    <%= prescription.medication_id %>
  </p>

</div>
